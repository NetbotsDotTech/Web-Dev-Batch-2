var age = 30;
switch (true) {
    case (age >= 15 && age <= 25):
        console.log("You are a Child")

        break;

    case (age >= 30 && age <= 40):
        console.log("You are a Adult")
        break;

    default:
        console.log("Your age is Invalid")

}